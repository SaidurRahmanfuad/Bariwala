<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.saidur.bariwala">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bariwala">
        <activity
            android:name=".ui.mvaratia.VaratiaMain"
            android:label="@string/title_activity_varatia_main"></activity>
        <activity android:name=".ui.mbariwala.home.house.CreateHouseActivity" />
        <activity android:name=".ui.registration.Registration" />
        <activity android:name=".ui.splashwelcome.Welcome" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.splashwelcome.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.mbariwala.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Bariwala.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>